# Архитектура

## Контекст

Официальный Telegram-клиент для [MafiaTracker](../MS). Полностью отдельное
приложение: свой репозиторий, свой аккаунт PythonAnywhere, своя (минимальная)
БД либо вообще без БД. Взаимодействие с основным сайтом — **только через
его API**, никакого прямого доступа к его базе данных.

## Технологический выбор

Хостинг — PythonAnywhere, синхронная WSGI-модель (запрос-ответ, без вечно
работающего процесса). Приём сообщений от Telegram — **вебхук**, не
long-polling (long-polling требует постоянного цикла — Always-on Task,
недоступно/дорого на простом тарифе).

Библиотека — **`pyTelegramBotAPI` (`telebot`)**, синхронная. `aiogram` и
`python-telegram-bot` v20+ — async-first, заточены под собственный event
loop; встраивать их в синхронный Flask-запрос на каждый вебхук-вызов
возможно, но не по их естественной логике. `telebot` штатно поддерживает
паттерн "Flask-роут получает JSON → `bot.process_new_updates(...)`" и
держит тот же синхронный стиль, что и весь основной проект.

## Слои

```
Telegram/сайт → Flask-роут → handlers/ → services/ → api_client/ → основной сайт
                                  ↓
                              presenters/ (чистые функции, без сети)
```

- **handlers/** — тонкие: разобрать апдейт/callback, вызвать `services`,
  отдать результат `presenters`, отправить/отредактировать сообщение.
  Никакой бизнес-логики, никаких прямых HTTP-вызовов.
- **presenters/** — чистые функции `данные → (текст, клавиатура)`. Ноль
  I/O — тестируются без мока сети и без реального токена бота (см.
  `tests/test_presenters_start.py`).
- **services/** — бот-специфичные правила ("не привязан — предложи
  привязать"), склейка `handlers` ↔ `api_client`.
- **api_client/** — единственное место, знающее про HTTP к основному
  сайту. Типизированные исключения (`ApiTimeout`, `ApiUnauthorized`,
  `ApiNotFound`, `ApiServerError`) вместо голых `requests.exceptions` —
  хендлеры ловят их и отвечают дружелюбным сообщением, не трейсбеком.
- **webhooks/** — обработка входящих `POST /events/<type>` от основного
  сайта (fire-and-forget уведомления о событиях). Пока не реализовано —
  появится вместе с соответствующими фичами на стороне основного сайта.

## Привязка аккаунта — без своей таблицы

Привязка происходит **целиком на основном сайте** через официальный
Telegram Login Widget (пользователь жмёт кнопку в профиле на сайте,
подтверждает в Telegram — без захода в диалог с ботом заранее). Значит:

- Единственный источник правды о привязке — колонка `Player.telegram_id`
  на основном сайте.
- **У бота НЕТ собственной таблицы "telegram_id ↔ player_id".** При каждом
  входящем сообщении бот спрашивает у API `GET /api/v1/bot/resolve?
  telegram_id=...` — кто это. Меньше движущихся частей, невозможен
  рассинхрон двух копий одних и тех же данных.
- Локальная БД у бота появится только если что-то по-настоящему
  бот-специфичное потребует состояния между запросами, которое не влезает
  в `callback_data` (например, многошаговый диалог) — по факту
  необходимости, не заранее.

## Безопасность

Три разных направления доверия, не путать:

1. **Telegram → бот** — секретный сегмент пути (`/telegram/webhook/<64
   случайных символа>`) + `secret_token`, который Telegram сам присылает
   в заголовке `X-Telegram-Bot-Api-Secret-Token` на каждый вызов (задаётся
   при регистрации вебхука). Проверка — `bot/security.py::verify_telegram_secret`.
2. **Бот → основной сайт** — один серверный секрет
   (`MAIN_API_SERVICE_TOKEN`, `Authorization: Bearer`), доказывающий "это
   наш бот-сервер", плюс `telegram_id` как параметр запроса — сайт сам
   резолвит его в игрока. Секрет никогда не виден пользователю бота.
3. **Основной сайт → бот** (fire-and-forget уведомления о событиях) —
   общий HMAC-секрет (`INCOMING_EVENT_SECRET`), подпись тела запроса в
   заголовке `X-Signature`, сверяется константным сравнением. Проверка —
   `bot/security.py::verify_event_signature`.
4. **Telegram Login Widget на основном сайте** (это про сайт, не про
   бота, но упомянуто здесь для полноты картины) — верификация подписи
   виджета требует секрет `SHA256(TELEGRAM_BOT_TOKEN)`, то есть основному
   сайту тоже нужен `TELEGRAM_BOT_TOKEN` — требование протокола Telegram,
   не архитектурный выбор этого проекта.

## Обработка ошибок

`bot/__init__.py::create_app()` оборачивает `process_new_updates` в
try/except: ошибка внутри хендлера (в т.ч. реальный сбой Telegram API при
отправке) логируется, но HTTP-ответ Telegram'у всегда `200` — иначе
Telegram будет ретраить один и тот же апдейт бесконечно. Ошибки при
обращении к API основного сайта ловятся в хендлерах по типизированным
исключениям `api_client.exceptions` → дружелюбное сообщение пользователю.

## Конфигурация и секреты

`bot/config.py` — тот же стиль, что `app/config.py` основного проекта:
плоский `os.environ.get()`, обязательные переменные падают с понятной
`RuntimeError`, если не заданы — без тихого fallback. **Важно**: импорт
`config_map` происходит ВНУТРИ `create_app()`, не на уровне модуля —
иначе любой импорт `bot.*` (в т.ч. из тестов) требовал бы реальных
секретов просто из-за того, что Python исполняет `__init__.py` пакета при
импорте любого его подмодуля. Та же ловушка уже была учтена в
`app/__init__.py` основного проекта.

Переменные — см. [.env.example](.env.example).

## Кеширование

In-process TTL-кэш (появится вместе с первыми read-heavy эндпоинтами —
лидерборд, каталог магазина). Персональные данные (баланс, профиль) —
всегда без кэша. Redis/Memcached не нужны на этом масштабе.

## Логирование

Стандартный `logging`, структурированные записи. `bot/logging_config.py`.

## Масштабируемость

Вебхук-модель = приложение стейтлес между запросами (кроме опционального
in-process кэша) — масштабируется ровно как обычный Flask-сайт.

## Обновления/деплой

Свой git-репозиторий, свой аккаунт PythonAnywhere, `deploy.sh` (git pull +
touch WSGI-файла) — тот же проверенный паттерн, что у основного проекта.

## Зависимости от основного проекта

Функционал бота дальше этого инкремента упирается в изменения основного
сайта, которые ещё не сделаны:

| Нужно на основном сайте | Для какой фичи бота | Статус |
|---|---|---|
| `Player.telegram_id` + Login Widget | Привязка аккаунта, `/resolve` | ✅ Готово (MS commit `98e6db1`) |
| `Game.round_number` + `TournamentService.generate_next_round()` | Авторассадка следующего раунда | ✅ Готово (MS commit `ea0fe90`) |
| `BotNotifyService` + `INCOMING_EVENT_SECRET`/`BOT_EVENTS_URL` — событие `next-slot` | Уведомление "Твой слот — N" | ✅ Готово (MS commit `4d081fd`, бот `e57405b`) |
| `/api/v1/bot/resolve`, `/profile`, `/stats`, `/compare`, `/account/unlink` | Резолв привязки, профиль, статистика, сравнение, отвязка | ✅ Готово и подключено (MS `e6af578`, бот `203b111`) |
| `/api/v1/bot/ratings`, `/history`, `/economy/*`, `/achievements`, `/titles` | Рейтинг, история, экономика, достижения, титулы | ✅ Готово и подключено (MS `edecd7a`, бот `f49b4a2`) |
| `/api/v1/bot/tournaments`, `/tournaments/<id>` | Турниры (только просмотр) | ✅ Готово и подключено (MS `3111b23`, бот `8171991`) |
| Fantasy-эндпоинты (`/api/v1/bot/fantasy/*`) | Fantasy — полное управление пиками из бота | ✅ Готово и подключено (MS `4b5fa90`, бот `a6ad588`) |
| Остальные fire-and-forget события (завершение игры, достижение/титул, перекуп Mythic/Ultra, fantasy, подарок, сезонная награда) | Остальные проактивные уведомления | ✅ Готово и подключено (MS `a905b21`, бот `6c6c9cc`) |
| `/api/v1/bot/players/search` | Инлайн-режим (`@bot Имя` в любом чате) | ✅ Готово и подключено (MS `b57f00c`, бот `5cd1d9a`) |

**Обновление (MafiaTracker 2.0 rewrite, см. CHANGELOG.md):** приведённая
выше таблица и текст ниже описывают исходный, command-driven v1-клиент
(профиль/статистика/рейтинг/турниры-только-просмотр/Fantasy-базовый/
сравнение). Он полностью замещён инлайн-клиентом с разделами Мой кабинет,
Рейтинги, Турниры (включая серийные), Fantasy (полный, с сериями/группами/
practice), Магазин, Инвентарь, Подарки, Сравнить игроков, Уведомления —
см. секции "Навигация" и "Новые домены" ниже. **"Вся полировка завершена"
было верно только для v1-набора функций** — оно НЕ означало полное
покрытие сайта; этот пробел (Fantasy без серий, отсутствие магазина/
инвентаря/подарков/настроек уведомлений, синхронный небезопасный outbox,
утечка скрытого рейтинга через Bot API) — именно то, что закрыл этот
переписывающий инкремент. Реальный деплой на PythonAnywhere по-прежнему
остаётся следующим шагом.

## Навигация (инлайн-клиент)

Постоянная Reply Keyboard сведена к одной кнопке `🏠 Меню` — весь
остальной интерфейс инлайн, редактирует одно и то же сообщение
(`bot.edit_message_text`), а не плодит новые. Единая версионированная
схема `callback_data`: `v1:<домен>:<действие>[:<аргументы>]`
(`bot/keyboards/nav.py::cb/parse_cb/is_cb`) — префикс версии позволяет
завтра ввести `v2:` для домена с несовместимым форматом аргументов, не
ломая уже отправленные (старые) сообщения с `v1:`-кнопками. В
`callback_data` — только короткие ID/enum-значения, никогда не цены,
права или другие доверенные данные: любое мутирующее действие всё равно
заново резолвится и проверяется на сайте.

Единый способ "куда вернуться" — не стек навигации (не хранится между
запросами), а то, что каждый экран сам знает своего логического родителя
(например, экран товара магазина всегда знает, что вернётся в список
той же категории) — тот же принцип, что уже использовался для
`tourn:<id>`/`fantasy_my:<id>` до этой переписки, просто теперь применён
единообразно везде через `add_nav_footer(markup, back_target=...)`.

`bot/dispatch.py::guarded_callback(bot, sensitive=False)` — декоратор для
`@bot.callback_query_handler`, которым обёрнут почти каждый новый хендлер:
проглатывает декоративную кнопку-индикатор страницы (`v1:noop`, у неё
отдельный собственный хендлер в `handlers/menu.py::handle_noop_callback`,
т.к. её саму ни один доменный `is_cb(...)` не матчит), блокирует двойное
нажатие sensitive-действия (покупка/подарок/создание драфта) на TTL через
блокировку в `bot/storage.py`, и гарантирует ровно один
`answer_callback_query` на любом пути, до которого хендлер сам не дошёл
(необработанное исключение, `ApiError`).

## Локальное состояние — `bot/storage.py`

Первая настоящая локальная БД бота (SQLite-файл, `bot/data/bot.db`,
переопределяется `BOT_DB_PATH`) — до этого ARCHITECTURE.md обещал "появится
по факту необходимости"; необходимость появилась сразу с тремя вещами:

1. **FSM многошаговых сценариев** (сейчас — отправка подарка: выбор
   предмета → ник получателя → поиск → заметка → подтверждение).
2. **Идемпотентность**: блокировка двойного нажатия (`try_acquire_action_lock`)
   и дедупликация повторного outbox-события от сайта по `event_id`
   (`is_event_processed`/`mark_event_processed`).
3. **Настройки уведомлений по категориям** — чисто UX-настройка бота
   (не бизнес-правило сайта), поэтому уместно хранить именно у бота.

Выбор — SQLite через стандартный `sqlite3`, не Redis: бот — один
небольшой Flask/WSGI-процесс без уже поднятой инфраструктуры; SQLite не
требует нового сервиса и переживает рестарт процесса (в отличие от
`dict` в памяти). Если бот когда-нибудь будет масштабирован на несколько
одновременных инстансов, делящих это состояние, — вот сигнал
пересмотреть выбор в пользу Redis/Postgres.

## Deep links

`bot/deeplink.py::resolve_deep_link(payload, telegram_id)` — `/start
<payload>` открывает конкретный экран (игра `g<id>`, турнир `t<id>`,
вечер серии `ev<tid>_<sid>`, Fantasy `fd<tid>`/`fs<tid>_<sid>`, сезон
`season<id>`, подарки `gift`) вместо всегда-главного-меню. Payload не
использует `:` (ограничение Telegram на алфавит `/start`-параметра) —
отдельная, более простая схема, чем `callback_data`. Ссылки на сайте
(`games/detail.html`, `tournaments/detail.html`, хелпер
`telegram_deep_link()` в `app/__init__.py` основного проекта) генерируют
эти payload'ы.

Telegram Web App сознательно не реализован — ни один из построенных
сценариев (каталог, таблицы, Fantasy-пики) не требует его: пагинация
инлайн-кнопками справляется. Строить его "про запас" было бы нарушением
собственного правила спеки ("только там, где обычные кнопки реально не
справляются").

## Надёжность доставки уведомлений (outbox, сторона MS)

Раньше `BotNotifyService.send_event` делал синхронный `requests.post`
прямо из потока запроса сайта — недоступность бота либо тормозила
основной сценарий (завершение игры, покупку), либо просто теряла
уведомление без ретрая. Теперь `send_event`/`notify_player` — тонкая
обёртка над `NotifyOutboxService.enqueue()` (см. MS,
`app/services/notify_outbox_service.py`): событие сохраняется в таблице
`notify_outbox_events`, доставка — отдельным шагом
(`NotifyOutboxService.drain()`, вызывается `flask outbox-drain` из cron
или опциональным фоновым потоком при `OUTBOX_WORKER_ENABLED=true`) с
экспоненциальной паузой между попытками и атомарным захватом строки
(`UPDATE ... WHERE status='pending'`), безопасным при нескольких
воркерах/потоках одновременно.

Бот дедуплицирует повтор по `event_id` (заголовок `X-Event-Id`,
`bot/storage.py::is_event_processed`) — retry никогда не даёт второе
сообщение в Telegram.

**Явное ограничение**: это не полностью атомарный transactional outbox —
`enqueue()` коммитит свою отдельную короткую транзакцию, не в одной
транзакции с бизнес-операцией, которая его вызвала. Крах в этом узком
окне всё ещё теоретически может потерять событие. Полностью атомарный
outbox потребовал бы протащить общую сессию через каждую точку вызова
(достижения, титулы, подарки, fantasy, экономика) — сознательно оставлено
как следующий шаг, а не сделано в этом инкременте.

Постоянное Reply-меню (`bot/presenters/menu.py`, `bot/handlers/menu.py`)
прикрепляется к `/start` и не требует отдельной таблицы состояний: каждая
кнопка (кроме «🎯 Fantasy», которая требует ID турнира и просто показывает
подсказку с командами) напрямую переиспользует уже существующий командный
хендлер — новой бизнес-логики не появилось, только текстовый роутинг вместо
командного (`bot.message_handler(func=lambda m: m.text == "...")` вместо
`commands=[...]`).

Готовность первой партии `/api/v1/bot/*` теперь означает не только "MS
отвечает", но и что бот реально их вызывает: `/start` резолвит вызывающего
по-настоящему, `/me` показывает карточку профиля, `/stats` — статистику,
`/compare <id>` — сравнение. Команда `/unlink` (с инлайн-подтверждением
Да/Отмена — первый `callback_query_handler` в проекте) реально вызывает
`account/unlink`.

Инлайн-режим (`bot/handlers/inline.py`, `@bot.inline_handler`) отвечает на
`@bot Имя` в любом чате: ищет игроков через `players/search`, строит
`InlineQueryResultArticle` на каждого (`presenters/players.py`) —
намеренно без похода за полной карточкой профиля на кандидата (инлайн-
запрос должен успевать ответить на каждое нажатие клавиши, лишний
API-вызов на кандидата не окупается).

Готовность авторассадки означает: `generate_next_round` считает и
возвращает (`ServiceResult.data`) всё нужное для уведомления —
`assignments` (player_id/game_id/table_number/seat_number/round_number на
каждого) и `resting_player_ids`.

Готовность `next-slot`-события означает: `finish_game` на MS уже реально
шлёт его боту сразу после `generate_next_round` (`app/services/
bot_notify_service.py::BotNotifyService.send_event`), а
`bot/webhooks/events.py::dispatch` уже реально его обрабатывает и шлёт
"Твой слот — N" через `TeleBot.send_message` — весь путь end-to-end
рабочий и проверен между двумя реально запущенными серверами. Остальные
типы событий (см. таблицу выше) используют тот же `BotNotifyService` +
тот же `EVENT_HANDLERS`-реестр — `next-slot` теперь готовый шаблон для
них, а не то, что нужно проектировать с нуля.

Каждый пункт реализуется отдельным согласованным инкрементом на стороне
основного репозитория (MS), не здесь.
